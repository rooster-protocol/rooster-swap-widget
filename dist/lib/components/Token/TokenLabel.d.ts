import { Token } from '../../types';
interface TokenLabelProps {
    token: Token;
    size?: number;
    ve?: boolean;
    asterisk?: boolean;
}
export default function TokenLabel({ token, size, ve, asterisk, }: TokenLabelProps): import("react/jsx-runtime").JSX.Element;
export {};
