import { SxProps, Theme } from '@mui/material';
export declare const CTAButton: ({ onClick, isLoading, disabled, text, tooltip, styles, }: {
    onClick: () => void;
    isLoading?: boolean;
    disabled?: boolean;
    text: string;
    tooltip?: string;
    styles?: SxProps<Theme>;
}) => import("react/jsx-runtime").JSX.Element;
