import { ModalStep } from '../../../types';
export declare const StepperModalFrame: ({ steps, activeStep, }: {
    steps: ModalStep[];
    activeStep: number;
}) => import("react/jsx-runtime").JSX.Element;
